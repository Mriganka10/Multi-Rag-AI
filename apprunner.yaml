version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building CA Agentic AI RAG container"
run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
    env: PORT
